[user]
	name = Rostyslav Ugryniuk
	email = 5703885+UgRoss@users.noreply.github.com
[includeIf "gitdir:~/"]
	path = ~/.gitconfig.local

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

